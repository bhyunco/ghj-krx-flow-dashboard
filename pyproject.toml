[project]
name = "ghj-krx-flow-dashboard"
version = "0.1.0"
description = "KRX investor flow dashboard with Flask"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "Flask>=3.0,<4.0",
    "pandas>=2.0",
    "openpyxl>=3.1",
]

[tool.vercel]
entrypoint = "api.index:app"
